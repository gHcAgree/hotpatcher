diff --git a/sample_functions.c b/sample_functions.c
index a7be92c..583e32d 100644
--- a/sample_functions.c
+++ b/sample_functions.c
@@ -8,7 +8,8 @@ int hello(int user)
     sprintf(buf, "%s%d", "goodbye ", user);
 	hdebug(buf);
 
-    printf("CHANGED\n");
+    sprintf(buf, "%s", "hahaha");
+    newdebug(buf);
 
 	return 111;
 }
